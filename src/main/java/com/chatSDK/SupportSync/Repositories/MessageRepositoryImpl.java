package com.chatSDK.SupportSync.Repositories;


public class MessageRepositoryImpl {

//    private final MessageRepository messageRepository;
//
//    public MessageRepositoryImpl(MessageRepository messageRepository) {
//        this.messageRepository = messageRepository;
//    }
//
//    public List<Message> findByChatSessionId(Long chatSessionId) {
//        // Fetch all messages and filter by chat session ID
//        return messageRepository.findAll()
//                .stream()
//                .filter(message -> message.getChatSession() != null && message.getChatSession().getId().equals(chatSessionId))
//                .collect(Collectors.toList());
//    }
//
//    public long countByChatSessionId(Long chatSessionId) {
//        return messageRepository.findAll()
//                .stream().filter(message->message.getChatSession()!=null && Objects.equals(message.getChatSession().getId(), chatSessionId)).count();
//    }
//
//    public void save(Message message) {
//        messageRepository.save(message);
//    }
//
//    public void delete(Long messageId) {
//        messageRepository.deleteById(messageId);
//    }
}

